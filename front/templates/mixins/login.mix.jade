mixin userLogin
  form.col-xs-12.col-md-6.col-md-offset-3(method="post", action="/login")
    .row
      .col-xs-12
        label(for="login-email") Email
        input(type="email", maxlength="120", placeholder="Enter your email", name="email", id="login-email")
    .row
      .col-xs-12
        label(for="login-password") Password
        a.pull-right.forgot-password(href="/forgot") Forgot your password?
        input(type="password", maxlength="120", placeholder="Enter your password", name="password", id="login-password")
    .row
      .col-xs-12
        input(type="hidden", value="#{_csrf}", name="_csrf")
        button.btn.btn-success.btn-lg.btn-block(type="submit") Login!
